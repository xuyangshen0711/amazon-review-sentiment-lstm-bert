# CS6120 Final Project — User Manual

## Overview

This project compares three LSTM-based sentiment classifiers and one BERT classifier
on Amazon product reviews. The best-performing model is the **Weighted BiLSTM**
(`lstm_weighted/`), which uses bidirectional LSTM, balanced sampling, and
class-weighted loss to handle the positive/negative class imbalance.

---

## Project File Structure

```
LSTM/
├── lstm_unweighted/
│   ├── lstm_pipeline_unweighted.py   # Baseline unweighted BiLSTM
│   └── model.pt                      # Trained checkpoint
│
├── lstm_weighted/
│   ├── lstm_pipeline_weighted.py     # Improved weighted BiLSTM (best model)
│   ├── demo.py                       # ← Interactive demo for the best model
│   └── model.pt                      # Trained checkpoint (includes vocab)
│
├── lstm_simple_weighted/
│   ├── lstm_pipeline_simple_weighted.py  # Simple LSTM with class weights
│   └── model.pt                          # Trained checkpoint
│
├── bert_pipeline.py                  # BERT fine-tuning pipeline
├── best_bert_model.pt                # BERT trained checkpoint
├── demo.py                           # Interactive demo for BERT
│
└── USER_MANUAL.md                    # This file
```

---

## 1. Environment Setup

### Prerequisites
- Python 3.8 or higher
- pip

### Step 1 — Install Python dependencies

```bash
pip install torch torchvision torchaudio
pip install transformers
pip install numpy pandas scikit-learn matplotlib nltk
```

### Step 2 — Download NLTK stopwords (run once)

```bash
python3 -c "import nltk; nltk.download('stopwords')"
```

### Step 3 — Prepare data files

Place the following in **any** of these locations (scripts search all of them):
- Inside the relevant pipeline folder (e.g., `lstm_weighted/`)
- In the project root (`LSTM/`)
- At `~/Desktop/CS6120/Presentation/`

Required files:
| File | Description |
|------|-------------|
| `Software_5.json.gz` | Amazon Software review dataset |
| `glove_data/glove.6B.300d.txt` | GloVe 300-dimensional word embeddings |

> The BERT pipeline (`bert_pipeline.py`) does not require GloVe — it downloads
> the `bert-base-uncased` tokenizer automatically on first run.

---

## 2. Running the Training Pipelines

Each pipeline is self-contained. Navigate to the project root and run:

### Baseline Unweighted BiLSTM

```bash
cd lstm_unweighted
python3 lstm_pipeline_unweighted.py
```

### Weighted BiLSTM (Best Model)

```bash
cd lstm_weighted
python3 lstm_pipeline_weighted.py
```

### Simple Weighted LSTM

```bash
cd lstm_simple_weighted
python3 lstm_pipeline_simple_weighted.py
```

### BERT Fine-tuning

```bash
python3 bert_pipeline.py
```

Each pipeline saves the following outputs into its own folder:
- `model.pt` — trained model weights (+ vocab for LSTM pipelines)
- `loss_curves.png` — training/validation loss curves
- `confusion_matrix.png` — test set confusion matrix
- `test_metrics.txt` — detailed classification metrics
- `model_summary.txt` — model size, speed, and performance summary

---

## 3. Running the Demo (Recommended)

The demo loads a pre-trained model and lets you interactively test it without
re-running training.

### BiLSTM Demo (Best Model)

```bash
cd lstm_weighted
python3 demo.py
```

**Requirements:** `model.pt` must exist in `lstm_weighted/` and must have been
generated by the updated `lstm_pipeline_weighted.py` (which saves the vocabulary
inside the checkpoint). If you have an older `model.pt`, re-run the training
pipeline once.

### BERT Demo

```bash
python3 demo.py
```

**Requirements:** `best_bert_model.pt` must exist in the project root.

Both demos will:
1. Print predictions for 5 built-in sample reviews
2. Enter an interactive mode where you can type your own review text
3. Type `quit` (or `exit` / `q`) to exit

---

## 4. Expected Output (BiLSTM Demo)

```
============================================================
  BiLSTM Sentiment Analysis — Amazon Reviews Demo
============================================================
  Device : cpu
  Model  : Bidirectional LSTM  (weighted / fine-tuned)
  Weights: model.pt
============================================================

Loading model... done.
  Vocab size       : 28,450
  Best threshold   : 0.413

────────────────────────────────────────────────────────────
SAMPLE PREDICTIONS
────────────────────────────────────────────────────────────

[1] This software is absolutely fantastic...
    => Prediction : Positive  (confidence: 94.32%)

[2] Terrible product. It crashed my computer twice...
    => Prediction : Negative  (confidence: 91.87%)
...

────────────────────────────────────────────────────────────
INTERACTIVE MODE  (type 'quit' to exit)
────────────────────────────────────────────────────────────

Enter a review: Great value for the price!
=> Prediction : Positive  (confidence: 88.41%)
```

---

## 5. Hardware Acceleration

All scripts automatically select the best available device:
- **CUDA** (NVIDIA GPU) — fastest
- **MPS** (Apple Silicon GPU) — fast on M1/M2/M3 Macs
- **CPU** — default fallback, slower training but fully functional

No manual configuration is needed.

---

## 6. Troubleshooting

| Problem | Solution |
|---------|----------|
| `FileNotFoundError: Software_5.json.gz` | Place the dataset file in the script's folder or project root |
| `FileNotFoundError: glove_data` | Place the `glove_data/` directory in the script's folder or project root |
| `KeyError: 'vocab'` in demo | Re-run `lstm_pipeline_weighted.py` to regenerate `model.pt` |
| `ModuleNotFoundError: transformers` | Run `pip install transformers` |
| BERT download very slow | First run downloads ~440 MB of model weights — normal behavior |
